/**
 * Where the résumé lives from the browser's point of view. Client components
 * and the route handler that streams the bytes share these, so the link, the
 * download name and the response header cannot drift apart.
 */
export const RESUME_PATH = "/resume.pdf";

export const RESUME_FILENAME = "Shreyas_Patange_Resume.pdf";
